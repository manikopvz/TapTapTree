import{UPGRADES}from'./config.js';export class Economy{constructor(s){this.s=s}cost(id){const u=UPGRADES.find(x=>x.id===id);return Math.floor(u.cost*u.scale**(this.s.up[id]||0))}mult(){return(1+this.s.seeds*.12)*(1+(this.s.up.tap||0))}tap(){return this.mult()*(1+this.s.combo*.01)*(this.s.frenzy>0?10:1)}aps(){return(this.s.up.auto||0)*(1+this.s.seeds*.12)*(this.s.frenzy>0?5:1)}buy(id){const c=this.cost(id);if(this.s.apples<c)return false;this.s.apples-=c;this.s.up[id]=(this.s.up[id]||0)+1;return true}rebirthReward(){return Math.floor(Math.sqrt(this.s.apples/25000))}}