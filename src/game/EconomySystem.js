import{upgrades}from'./data.js';export class EconomySystem{constructor(s){this.s=s}cost(id){let u=upgrades.find(x=>x.id===id);return Math.floor(u.base*u.scale**(this.s.up[id]||0))}global(){return 1+this.s.seeds*.15}tap(){return(1+(this.s.up.tap||0))*this.global()*(1+Math.min(150,this.s.combo)*.01)*(this.s.frenzy>0?10:1)}aps(){return(this.s.up.auto||0)*this.global()*(this.s.frenzy>0?5:1)}buy(id){let c=this.cost(id);if(this.s.apples<c)return false;this.s.apples-=c;this.s.up[id]=(this.s.up[id]||0)+1;return true}rebirth(){return Math.floor(Math.sqrt(this.s.apples/30000))}}