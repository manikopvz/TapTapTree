import fs from'node:fs';import path from'node:path';const roots=['src','assets'];let bad=[];function walk(p){for(const e of fs.readdirSync(p,{withFileTypes:true})){let f=path.join(p,e.name);if(e.isDirectory())walk(f);else if(/\.svg$/i.test(e.name))bad.push(f)}}roots.forEach(r=>fs.existsSync(r)&&walk(r));if(bad.length){console.error('SVG prohibited:',bad);process.exit(1)}for(const f of ['assets/images/apple.webp','assets/images/golden-apple.webp','assets/images/leaf.webp'])if(!fs.existsSync(f)){console.error('Missing raster asset',f);process.exit(1)}console.log('Raster art verification passed. No SVG files found.')}